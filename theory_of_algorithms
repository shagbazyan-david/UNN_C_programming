// Example program
#include <stdio.h>

int main()
{

    /*
    Алгоритмы и язык Си.

        1. Конечность.
                      Алгоритм должен обеспечивать получение результата после конечного числа шагов. 
                      При этом результатом считается не только обусловленный постановкой задачи ответ, 
                      но и вывод о невозможности продолжения решения данной задачи
        2. Дискретность. (состоит из шагов)
                        Процесс решения задачи должен быть разбит на последовательность отдельных шагов-команд, 
                        которые выполняются одна за другой. 
                        Только после завершения одной команды начинается выполнение следующей.
        3. Детерменизм. (отсутствие двоякой интерпретации шагов)
                        Каждый шаг алгоритма должен быть строго определён и не допускать различных толкований. Также строго определяется порядок выполнения шагов. 
                        Благодаря этому:
                        при любом исполнителе для одних и тех же исходных данных при выполнении одной и той же цепочки команд выдаётся одинаковый результат;
        4. Массовость. sin(60), sin(45)
                Алгоритм должен быть разработан в общем виде и применим для решения всего класса задач, различающихся только исходными данными. 
                Например: алгоритм решения квадратного уравнения должен быть применим к любым квадратным уравнениям, 
                независимо от значений коэффициентов;  алгоритм перехода улицы должен быть применим в любом месте улицы;
         

          Алгоритм - это Машина Тьюринга

        Эквивалентные алгоритмы = Решают одну и ту же задачу.

        Alg1   Alg2

        Массив = [45  23  56  4  345  19  40]
        Есть ли N в массиве.
        Линейный поиск (перебор всех элементов массива)
        O(N) - Линейная сложность.
        
        Найти сумму первых двух элементов массива, 
        Распечатать первый элемент массива
        O(1) - Сложность константа.

        -------------------------------
        Бинарный поиск.
        4   6  10  20  30  40  100  467  1000
        N = 30
          Шаг1. Смотрим в середниу массива.
          Шаг2. Если элемент равен исходному N, то заканчиваем посик, он найден. Иначе переходим к Шагу3.
          Шаг3. Если элемент массива больше чем N, то переходим к Шагу4, иначе к Шагу5. 
          Шаг4. Рассматриваем только левую часть массива , если левая часть отсутствует то заканчиваем поиск,
                сообщив, что элемент не найден, иначе и переходим к Шагу1.
          Шаг5. Рассматриваем только правую часть массива , если правая часть отсутствует то заканчиваем поиск,
                сообщив, что элемент не найден, иначе и переходим к Шагу1.

         2*2*2*2 ... = N
          2^k = N
            Сложность логарифмическая. O(log(N))

          Бывают разные сложности : O(N^2)  , O(N^3), O(NlonN) и т д
        Сложность показывает как увеличивается работа с увелечением входных данных. Т е как резко растет функция описывающая данную связь.
        Си.
            1. Алгоритмический язык.

                Неалгоритмические языки HTML/CSS/1C/XML/bash/SQL

            2. Компилируемый язык.

            3. Язык 3-ьего поколения.
                1. Машинный код.
                2. Assembles.
                3. C/C++, Fortran, Pascal, Java .....
                4. HTML/CSS/SQL/Python(ML)
                5. Естественный язык (человеческий)

            4. Существую как статическая , так  динамическая типизация.
                C/C++ - Строгая типизация (статическая)
                  int a = 12;
    */
            /*


    1 байт      char  | unsigned char
                        00000000 - 0
                        11111111 - 255
    2 байта     short | unsigned short  0... 2^16 - 1

    4 байта     int   | unsigned int  0.....2^32 - 1

    4 байта     long     | unsigned long
                long int | unsigned long int

    8 байта     long long     | unsigned long long
                long long int | unsigned long long int

    float f = 3.14
    double d = 4.567;
    */

    int a = 12, b = 67;
    printf("hello world a = %d b = %d \n", a, b);
    printf("%6d %06d\n", 782, 782);
    printf("%-6d%-6d\n", 782, 782);
    printf("%-6d%-6d\n", 782, 782);

    printf("%X", 34);

    //printf("\tHello world\n\tMy name is Dima");


    return 0;
}
